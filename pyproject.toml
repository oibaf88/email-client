[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py312"
line-length = 100
exclude = ["docker-data", "flask_session"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
